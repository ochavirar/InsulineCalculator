import 'package:flutter/material.dart';
import 'package:insuline_calculator/widgets/sensibility_widgets/sensibility_container.dart';

class SensibilitySettings extends StatelessWidget {
  const SensibilitySettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const SensibilityContainer();
  }
}